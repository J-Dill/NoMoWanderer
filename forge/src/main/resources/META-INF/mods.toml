modLoader="javafml"
loaderVersion="${version_range_forge}"
issueTrackerURL="${mod_issues}"
license="MIT License"
[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
credits="Big shoutout to computers, without you I would have no job."
authors="${mod_authors}"
description="${mod_description}"
[[dependencies.${mod_id}]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version}"
    ordering="NONE"
    side="BOTH"
[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version}"
    ordering="NONE"
    side="BOTH"
